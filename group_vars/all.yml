---
ansible_managed: "Ansible managed: {{ template_path }} by {{ ansible_facts['user_id'] | default(ansible_user) }} on {{ inventory_hostname }}"

git_user_email: "{{ vault_git_user_email }}"
git_user_name: "{{ vault_git_user_name }}"

homebrew_taps: []

homebrew_packages:
  - 1password-cli
  - ack
  - act
  - ansible
  - ansible-lint
  - bat
  - btop
  - coderabbit
  - dust
  - eza
  - fd
  - fzf
  - gh
  - gron # Make JSON greppable!
  - helm
  - htop
  - jq
  - k9s
  - kind
  - ktlint
  - kubectx
  - lazygit
  - micro
  - mise
  - MonitorControl
  - ncdu
  - pwgen
  - ripgrep
  - starship
  - television
  - tig
  - watch
  - worktrunk
  - yamllint
  - yq
  - zellij
  - zoxide
  - zsh
  - zsh-history-substring-search

homebrew_casks:
  - alt-tab
  - finetune
  - font-fira-code
  - font-hack-nerd-font
  - font-iosevka
  - font-monaspace
  - font-sf-mono
  - keepingyouawake
  - monitorcontrol
  - neovide
  - raycast
  - thaw@beta
  - zed
  - wezterm

config_files:
  - name: "Wez terminal config"
    src: "./templates/wezterm.lua.j2"
    dest: "~/.config/wezterm/wezterm.lua"
  - name: "Zsh config"
    src: "./templates/zshrc.sh"
    dest: "~/.zshrc"
  - name: "Zed config"
    src: "./templates/zed/settings.json"
    dest: "~/.config/zed/settings.json"
  - name: "Zed keymap"
    src: "./templates/zed/keymap.json"
    dest: "~/.config/zed/keymap.json"
  - name: "Vim config"
    src: "./templates/vimrc"
    dest: "~/.vimrc"
  - name: "Midnight commander INI config"
    src: "./templates/mc.ini"
    dest: "~/.config/mc/ini"
  - name: "Kitty config"
    src: "./templates/kitty.conf"
    dest: "~/.config/kitty/kitty.conf"
  - name: "Gemini config"
    src: "./templates/geminiSettings.json"
    dest: "~/.gemini/settings.json"
  - name: "Codex rules: Git"
    src: "./templates/codex/rules/git.rules"
    dest: "~/.codex/rules/git.rules"
  - name: "Codex rules: Helm"
    src: "./templates/codex/rules/helm.rules"
    dest: "~/.codex/rules/helm.rules"
  - name: "Codex rules: GitHub CLI"
    src: "./templates/codex/rules/gh.rules"
    dest: "~/.codex/rules/gh.rules"
  - name: "Codex rules: NPM"
    src: "./templates/codex/rules/npm.rules"
    dest: "~/.codex/rules/npm.rules"
  - name: "Firefox user.js"
    src: "./templates/firefox/user.js"
    dest: "{{ firefox_profile_dir }}/user.js"
  - name: "Starship config"
    src: "./templates/starship.toml"
    dest: "~/.config/starship.toml"
  - name: "Mise config"
    src: "./templates/mise.toml"
    dest: "~/.config/mise/config.toml"
  - name: "Pi skill: worktrunk"
    src: "./templates/pi/skills/worktrunk/SKILL.md"
    dest: "~/.pi/agent/skills/worktrunk/SKILL.md"
  - name: "Pi skill: gh-cli"
    src: "./templates/pi/skills/gh-cli/SKILL.md"
    dest: "~/.pi/agent/skills/gh-cli/SKILL.md"
  - name: "Pi skill: creating-pull-requests"
    src: "./templates/pi/skills/creating-pull-requests/SKILL.md"
    dest: "~/.pi/agent/skills/creating-pull-requests/SKILL.md"
  - name: "Pi skill: ansible"
    src: "./templates/pi/skills/ansible/SKILL.md"
    dest: "~/.pi/agent/skills/ansible/SKILL.md"
  - name: "Pi skill: brave-search"
    src: "./templates/pi/skills/brave-search/SKILL.md"
    dest: "~/.pi/agent/skills/brave-search/SKILL.md"
  - name: "Pi skill: new-relic"
    src: "./templates/pi/skills/new-relic/SKILL.md"
    dest: "~/.pi/agent/skills/new-relic/SKILL.md"
  - name: "Pi skill: gcp-gcloud"
    src: "./templates/pi/skills/gcp-gcloud/SKILL.md"
    dest: "~/.pi/agent/skills/gcp-gcloud/SKILL.md"
  - name: "Pi skill: kubernetes-skill"
    src: "./templates/pi/skills/kubernetes-skill/"
    dest: "~/.pi/agent/skills/kubernetes-skill/"
    render: false
  - name: "Pi global AGENTS.md"
    src: "./templates/pi/AGENTS.md"
    dest: "~/.pi/agent/AGENTS.md"
  - name: "Pi Plannotator config"
    src: "./templates/pi/plannotator.json"
    dest: "~/.pi/agent/plannotator.json"
  # @tintinweb/pi-subagents reads its own settings from here, not settings.json.
  - name: "Pi subagents config"
    src: "./templates/pi/subagents.json"
    dest: "~/.pi/agent/subagents.json"
  - name: "Pi commit message convention"
    src: "./templates/pi/COMMIT_MESSAGE_CONVENTION.md"
    dest: "~/.pi/agent/COMMIT_MESSAGE_CONVENTION.md"
  - name: "Pi markdown styleguide"
    src: "./templates/pi/STYLEGUIDE_MARKDOWN.md"
    dest: "~/.pi/agent/STYLEGUIDE_MARKDOWN.md"
  - name: "Pi prompt: tell"
    src: "./templates/pi/prompts/tell.md"
    dest: "~/.pi/agent/prompts/tell.md"
  - name: "Pi prompt: ask_me_questions"
    src: "./templates/pi/prompts/ask_me_questions.md"
    dest: "~/.pi/agent/prompts/ask_me_questions.md"
  - name: "Pi prompt: draft-pr"
    src: "./templates/pi/prompts/draft-pr.md"
    dest: "~/.pi/agent/prompts/draft-pr.md"
  # Stubs that disable the two built-in agents shipped by pi-subagents;
  # Explorer and Planner below replace them.
  - name: "Pi agent: Explore (disabled)"
    src: "./templates/pi/agents/Explore.md"
    dest: "~/.pi/agent/agents/Explore.md"
  - name: "Pi agent: Plan (disabled)"
    src: "./templates/pi/agents/Plan.md"
    dest: "~/.pi/agent/agents/Plan.md"
  - name: "Pi agent: Explorer"
    src: "./templates/pi/agents/Explorer.md"
    dest: "~/.pi/agent/agents/Explorer.md"
  - name: "Pi agent: Planner"
    src: "./templates/pi/agents/Planner.md"
    dest: "~/.pi/agent/agents/Planner.md"
  - name: "Pi agent: Researcher"
    src: "./templates/pi/agents/Researcher.md"
    dest: "~/.pi/agent/agents/Researcher.md"
  - name: "Pi agent: GithubScout"
    src: "./templates/pi/agents/GithubScout.md"
    dest: "~/.pi/agent/agents/GithubScout.md"
  - name: "Pi agent: LinearScout"
    src: "./templates/pi/agents/LinearScout.md"
    dest: "~/.pi/agent/agents/LinearScout.md"
  - name: "Pi agent: EnvironmentScout"
    src: "./templates/pi/agents/EnvironmentScout.md"
    dest: "~/.pi/agent/agents/EnvironmentScout.md"
  - name: "Pi agent: Reviewer"
    src: "./templates/pi/agents/Reviewer.md"
    dest: "~/.pi/agent/agents/Reviewer.md"
  - name: "Pi agent: Worker"
    src: "./templates/pi/agents/Worker.md"
    dest: "~/.pi/agent/agents/Worker.md"
  - name: "Pi skill: coderabbit"
    src: "./templates/pi/skills/coderabbit/SKILL.md"
    dest: "~/.pi/agent/skills/coderabbit/SKILL.md"
  - name: "Pi skill: herdr"
    src: "./templates/pi/skills/herdr/SKILL.md"
    dest: "~/.pi/agent/skills/herdr/SKILL.md"
  - name: "Herdr config"
    src: "./templates/herdr/config.toml"
    dest: "~/.config/herdr/config.toml"
  - name: "Television cable: channels"
    src: "./templates/television/cable/channels.toml"
    dest: "~/.config/television/cable/channels.toml"
  - name: "Television cable: dirs"
    src: "./templates/television/cable/dirs.toml"
    dest: "~/.config/television/cable/dirs.toml"
  - name: "Television cable: env"
    src: "./templates/television/cable/env.toml"
    dest: "~/.config/television/cable/env.toml"
  - name: "Television cable: files"
    src: "./templates/television/cable/files.toml"
    dest: "~/.config/television/cable/files.toml"
  - name: "Television cable: gh-prs"
    src: "./templates/television/cable/gh-prs.toml"
    dest: "~/.config/television/cable/gh-prs.toml"
  - name: "Television cable: git-log"
    src: "./templates/television/cable/git-log.toml"
    dest: "~/.config/television/cable/git-log.toml"
  - name: "Television cable: path"
    src: "./templates/television/cable/path.toml"
    dest: "~/.config/television/cable/path.toml"
  - name: "Television cable: recent-files"
    src: "./templates/television/cable/recent-files.toml"
    dest: "~/.config/television/cable/recent-files.toml"
  - name: "Television cable: text"
    src: "./templates/television/cable/text.toml"
    dest: "~/.config/television/cable/text.toml"

pi_agent_settings_path: "~/.pi/agent/settings.json"
pi_agent_mcp_servers_base:
  context-mode:
    command: "context-mode"
  context7:
    type: "streamable-http"
    url: "https://mcp.context7.com/mcp"
  intellij-idea:
    type: "streamable-http"
    url: "http://127.0.0.1:64342/stream"
    # IntelliJ IDEA's built-in MCP server (streamable-http) on localhost. The
    # IJ_MCP_SERVER_PROJECT_PATH header tells the IDE which open project to
    # target. Pi's MCP adapter interpolates ${VAR} from the environment at
    # connection time (there is no literal per-call substitution), so ${PWD}
    # resolves to the directory Pi was launched in — i.e. the worktree that
    # IDEA is opened on. Custom headers also disable OAuth auto-detection, so
    # no `auth` is needed for this local endpoint. If you switch the project
    # mid-session, run `/mcp reconnect intellij-idea` to re-resolve the header
    # from the new $PWD.
    headers:
      IJ_MCP_SERVER_PROJECT_PATH: "${PWD}"

# Host-specific MCP servers (e.g. work-only integrations) are added via
# pi_agent_mcp_servers_local in host_vars/<host>/vars.yml and merged in below.
pi_agent_mcp_servers: >-
  {{
    pi_agent_mcp_servers_base
    | combine(
        hostvars[inventory_hostname].get('pi_agent_mcp_servers_local', {}),
        recursive=True
      )
  }}

pi_agent_settings_packages:
  - "git:github.com/boga/pi-local-claude-loader"
  - "npm:@plannotator/pi-extension"
  - "npm:context-mode"
  - "npm:pi-mcp-adapter"
  - "npm:pi-mono-ask-user-question"
  - "npm:@tintinweb/pi-subagents"

# Settings overrides shared across hosts; per-host deltas (e.g. enabledModels)
# come from pi_agent_settings_overrides_local in host_vars/<host>/vars.yml.
pi_agent_settings_overrides_base:
  collapseChangelog: true
  enableInstallTelemetry: false

pi_agent_settings_overrides: >-
  {{
    pi_agent_settings_overrides_base
    | combine(
        hostvars[inventory_hostname].get('pi_agent_settings_overrides_local', {}),
        recursive=True
      )
    | to_json
  }}

# Model tiers used by the Pi agent templates in templates/pi/agents/.
# @tintinweb/pi-subagents has no settings-level agentOverrides map, so per-agent
# model and thinking live in the agent files themselves; each host supplies the
# three tiers in its vars.yml. Reviewer defaults to the deep tier with no
# CodeRabbit; a host overrides either in its own vars.yml.
pi_agent_model_reviewer: "{{ pi_agent_model_deep }}"
pi_agent_reviewer_coderabbit: false

# Whether this host can reach Linear at all. Planner only offers LinearScout as a
# delegation target when it can, so a host without the MCP server does not waste
# a spawn discovering that.
pi_agent_has_linear: >-
  {{
    'linear' in (
      pi_agent_mcp_servers_base
      | combine(hostvars[inventory_hostname].get('pi_agent_mcp_servers_local', {}))
    )
  }}

# Plannotator thinking levels are identical across hosts today; per-host
# overrides can be supplied via pi_plannotator_thinking_local.
pi_plannotator_thinking_base:
  planning: "xhigh"
  executing: "medium"
  reviewing: "xhigh"

pi_plannotator_thinking: >-
  {{
    pi_plannotator_thinking_base
    | combine(
        hostvars[inventory_hostname].get('pi_plannotator_thinking_local', {}),
        recursive=True
      )
  }}

pi_plannotator_planning_thinking: "{{ pi_plannotator_thinking.planning }}"
pi_plannotator_executing_thinking: "{{ pi_plannotator_thinking.executing }}"
pi_plannotator_reviewing_thinking: "{{ pi_plannotator_thinking.reviewing }}"
